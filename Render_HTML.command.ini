[Command]
Command="
  copyq:
    var text = copy_auto().trim()
    if (!text)
      abort()
    copy(mimeHtml, text)
    copySelection(mimeHtml, text)
    paste_auto()
"
GlobalShortcut=meta+h
InMenu=true
Input=text/plain
IsGlobalShortcut=true
Match=^\\s*<(!|html)
Name=Render HTML
