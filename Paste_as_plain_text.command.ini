[Command]
Command="
  copyq: 
    var text = str(clipboard()).trim()
    copy(text)
    copySelection(text)
    paste()
"
GlobalShortcut=ctrl+shift+v, ctrl+shift+p
Icon=
IsGlobalShortcut=true
Name=Paste as plain text
